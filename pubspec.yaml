# Root pub-workspace manifest. Members set `resolution: workspace`.
name: _flutterbits_workspace
publish_to: none

environment:
  # Pub workspaces require Dart >= 3.6 (= Flutter 3.27); see the package floor.
  sdk: '>=3.6.0 <4.0.0'

workspace:
  - packages/flutterwindcss
