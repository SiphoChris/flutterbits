# Root pub-workspace manifest. Members set `resolution: workspace`.
name: _flutterbits_workspace
publish_to: none

environment:
  # See packages/flutterwindcss/pubspec.yaml for the floor rationale (Dart 3.7).
  sdk: '>=3.7.0 <4.0.0'

workspace:
  - packages/flutterwindcss
  - packages/flutterwindcss/example
  - apps/example
